# Fntermux 项目指南

## 界面语言
- **强制要求：** 用户界面（UI）必须使用 **中文**。所有按钮、标签、提示信息和错误消息应以中文显示。

## 打包与环境
- **fnOS 兼容性：** 应用必须在 fnOS 的 web 桌面（fnweb）中作为原生应用正常运行。
- **路径处理：** 必须优先使用 `TRIM_APPDEST` 和 `TRIM_PKGVAR` 环境变量来定位二进制文件和数据目录。
